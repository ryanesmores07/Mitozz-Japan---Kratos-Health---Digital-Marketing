interface:
  display_name: "Mitozz Content Calendar"
  short_description: "Plan monthly Mitozz Instagram calendars from strategy, audience, and brand context."
  brand_color: "#6F8794"
  default_prompt: "Create or update the monthly Mitozz Instagram content calendar. For a new month, scaffold the local CSV first, draft the calendar into that file, then publish the matching Google Sheet in Drive when the month is ready."

policy:
  allow_implicit_invocation: true
