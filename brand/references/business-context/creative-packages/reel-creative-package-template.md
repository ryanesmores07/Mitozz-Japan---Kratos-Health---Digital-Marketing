# Reel Creative Package Template

Use this template when a calendar row format is `Reel`.

---

## Meta

- `date`:
- `format`: `instagram-reel`
- `campaign_name`:
- `calendar_row_ref`:
- `primary_persona`:
- `objective`:

## Creative Objective

State in 1-2 lines what the reel should achieve.

## Target Persona

Choose one primary persona.

## Format Decision

- `format`: `reel`
- `why_this_format`:

## Creative Direction

- `core_concept`:
- `angle`:
- `immediate_takeaway`:
- `emotional_effect`:
- `what_to_avoid`:

## Motion / Reel Strategy

- `reel_type`:
- `target_runtime_seconds`:
- `opening_hook_strategy`:
- `closing_cta_role`:
- `source_mode`:
  - `generated-images-only`
  - `mixed-generated-images-and-existing-footage`
  - `existing-footage-led`
- `motion_asset_mode`:
  - `still-images-only`
  - `still-images-plus-sora-clips`
  - `existing-clips-only`
- `text_overlay_strategy`:
  - `add-in-edit`
  - `bake-into-source-frame`
- `continuity_tokens`:
  - `subject identity`
  - `wardrobe`
  - `environment`
  - `palette`
  - `light direction`
  - `lens distance`
  - `bottle orientation`

## Runtime Rules

- Hard maximum: `60 seconds`
- Default target for most reels: `8-15 seconds`
- Preferred ranges by reel goal:
  - `hook / awareness`: `8-10 seconds`
  - `education / explainer`: `10-15 seconds`
  - `routine / lifestyle`: `8-12 seconds`
  - `product / trust`: `8-12 seconds`
  - `deeper education`: `15-30 seconds` only when the concept clearly benefits
- Use `30-60 seconds` only for strong high-retention concepts with a clear narrative payoff
- Do not go longer just because more footage exists
- The first `1-2 seconds` must always function as the hook zone
- Keep one main message per beat so the runtime stays tight

## Runtime Rules

- Hard maximum: `60 seconds`
- Default target for most reels: `8-15 seconds`
- Preferred ranges by reel goal:
  - `hook / awareness`: `8-10 seconds`
  - `education / explainer`: `10-15 seconds`
  - `routine / lifestyle`: `8-12 seconds`
  - `product / trust`: `8-12 seconds`
  - `deeper education`: `15-30 seconds` only when the concept clearly benefits
- Use `30-60 seconds` only for strong high-retention concepts with a clear narrative payoff
- Do not go longer just because more footage exists
- The first `1-2 seconds` must always function as the hook zone
- Keep one main message per beat so the runtime stays tight

## Layout / Structure

Document the reel scene-by-scene.

### Shot 01

- `Shot ID`:
- `Narrative role`:
- `Approx. duration`:
- `Source asset needed`:
- `Primary subject`:
- `Framing`:
- `Camera behavior`:
- `Expected motion`:
- `On-screen text`:
- `Transition behavior`:

### Shot 02

- `Shot ID`:
- `Narrative role`:
- `Approx. duration`:
- `Source asset needed`:
- `Primary subject`:
- `Framing`:
- `Camera behavior`:
- `Expected motion`:
- `On-screen text`:
- `Transition behavior`:

### Shot 03

- `Shot ID`:
- `Narrative role`:
- `Approx. duration`:
- `Source asset needed`:
- `Primary subject`:
- `Framing`:
- `Camera behavior`:
- `Expected motion`:
- `On-screen text`:
- `Transition behavior`:

### Optional Additional Shots

Repeat as needed up to a normal maximum of 6 shots.

## Copy Direction

- `hook_ja`:
- `supporting_copy_ja`:
- `caption_direction_ja`:
- `cta_ja`:
- `notes_on_tone`:

## Visual Direction

- `imagery_style`:
- `composition_rules`:
- `product_visibility_level`:
- `props_and_setting`:
- `styling_notes`:
- `what_not_to_show`:

## Brand / Messaging Check

- `premium trust-first positioning`:
- `mitochondria-first education`:
- `calm Japanese-premium execution`:
- `general-food-safe compliant messaging`:

## Nano Banana / Source Asset Handoff

- `asset_archetype`:
- `visual_intent`:
  - 
- `brand_guardrails`:
  - 
- `composition`:
  - 
- `reference_strategy`:
- `selected_image_references`:
  - `path`:
    - `role`:
    - `influence`:
- `variation_from_references`:
  - 
- `text_overlay`:
  - `allowed`:
  - `max_words`:
  - `tone`:
  - `headline_ja`:
  - `slides_ja`:
- `negative_prompts`:
  - 

## Source Asset Plan

List the exact source assets that must exist before the freelancer receives the brief.

- `shot-01`:
  - `prompt_filename`:
  - `source_asset_type`:
  - `creation_method`:
  - `asset_goal`:
  - `must_hold_continuity_with`:
- `shot-02`:
  - `prompt_filename`:
  - `source_asset_type`:
  - `creation_method`:
  - `asset_goal`:
  - `must_hold_continuity_with`:
- `shot-03`:
  - `prompt_filename`:
  - `source_asset_type`:
  - `creation_method`:
  - `asset_goal`:
  - `must_hold_continuity_with`:

## Optional Internal Sora Clip Plan

Use this section only when an approved still should be turned into a motion clip before the freelancer receives the packet.

### `shot-01`

- `use_sora`: `yes/no`
- `input_asset`:
- `duration_seconds`:
- `camera_motion`:
- `subject_motion`:
- `prompt`:
- `negative_prompt`:

### `shot-02`

- `use_sora`: `yes/no`
- `input_asset`:
- `duration_seconds`:
- `camera_motion`:
- `subject_motion`:
- `prompt`:
- `negative_prompt`:

### `shot-03`

- `use_sora`: `yes/no`
- `input_asset`:
- `duration_seconds`:
- `camera_motion`:
- `subject_motion`:
- `prompt`:
- `negative_prompt`:

## Freelancer Edit Blueprint

Write one exact editing instruction block per shot.

### `shot-01`

- `duration_seconds`:
- `edit_goal`:
- `source_assets`:
- `transition_in`:
- `transition_out`:
- `text_ja`:
- `editor_instruction`:

### `shot-02`

- `duration_seconds`:
- `edit_goal`:
- `source_assets`:
- `transition_in`:
- `transition_out`:
- `text_ja`:
- `editor_instruction`:

### `shot-03`

- `duration_seconds`:
- `edit_goal`:
- `source_assets`:
- `transition_in`:
- `transition_out`:
- `text_ja`:
- `editor_instruction`:

## Optional Image-To-Video Prompt Pack

Use this only when a still image will be converted into a short motion clip before editing.

### `shot-01`

- `source_image_path`:
- `target_duration_seconds`:
- `motion_goal`:
- `camera_behavior`:
- `subject_motion`:
- `prompt`:
- `negative_prompt`:

### `shot-02`

- `source_image_path`:
- `target_duration_seconds`:
- `motion_goal`:
- `camera_behavior`:
- `subject_motion`:
- `prompt`:
- `negative_prompt`:

### `shot-03`

- `source_image_path`:
- `target_duration_seconds`:
- `motion_goal`:
- `camera_behavior`:
- `subject_motion`:
- `prompt`:
- `negative_prompt`:

## Freelancer Handoff Summary

Use this to summarize the overall edit for the freelancer.

- `what_the_reel_must_communicate`:
- `how_the_edit_should_feel`:
- `what_the_editor_must_not_change`:
- `final_delivery_requirements`:

## Motion Guardrails

- 

## Transition Notes

- 

## Editor Notes

- `subtitle_style`:
- `music_direction`:
- `pace_notes`:
- `cta_hold_duration`:
- `final_export_notes`:
