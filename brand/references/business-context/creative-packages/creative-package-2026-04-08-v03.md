# Creative Package - 2026-04-08 v03

Cleaned-up production candidate for April 8.

Purpose:

- keep the approved April 8 message and CTA locked
- rebuild the feed on the cleaned-up shared typography + compositor system
- use the softer portrait-trust cover and stacked proof rhythm as the single production candidate

Locked variant decisions:

- `visual_engine`: `image-led cover + editorial body`
- `anchor_set`: `anchor-portrait-trust + anchor-editorial-whitespace + Set C`
- `dominant_set_behavior`: `portrait-trust opener + stacked trust-proof cards`
- `variation_strategy`: `replace the April 8 v01 three-column trust model with stacked proof cards and a softer cover mood, then promote that cleaned-up structure into one production lineage`
- `selected_set_images`: `anchor-portrait-trust-01.png`, `anchor-editorial-whitespace-01.png`, `Set C / Screenshot 2026-03-29 at 16.50.37.png`
- `variant_scope`: `design-only`
- `palette_variant`: `warm_editorial`
- `type_profile`: `humanist_sans`
- `source_lane`: `Nano-Banana-source-image`
- `source_strategy`: `fresh Nano Banana portrait-trust cover plate with protected left text zone, then compositor-built body slides`
- `fallback_source`: `fallback to the approved v01 cover plate only if the v03 cover fails the text-safe-zone or trust-tone test`
- `icon_strategy`: `none`
- `generated_visual_role`: `hero-visual`

Creative direction:

- keep the message: premium comes from trust, not surface polish
- keep the same approved frontend copy and CTA logic as April 8 `v01`
- soften the delivery so it feels less systems-led and more quietly human
- use the body slides to show trust as something layered, not simply listed
- use the shared cleanup system so typography, spacing, and optical centering follow the newest rules by default

Production layout:

1. warmer portrait-trust cover with a restrained lower trust band
2. editorial reframe card
3. stacked trust-proof cards instead of the current three-column model
4. practical reading rows
5. calm save close

## Story Support

- `format`: `Story support`
- `story_topic`: `本当に上質と感じるブランドとは？`
- `story_role`: `support the feed with a lighter trust-reading prompt, then route attention back to the carousel`
- `story_structure`:
  1. image-led opener tied to the approved feed cover plate
  2. short trust-reading frame using `説明 / 基準 / 姿勢`
  3. route-back frame pointing to the April 8 feed
- `story_rules`:
  - keep the set lighter than the carousel
  - reuse the April 8 trust plate only as a family opener, not as a duplicate cover
  - do not turn the story into another full explainer
  - keep frame-3 clearly directional
